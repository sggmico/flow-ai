# Claude Code 全局规范配置指南

本指南将帮助你在 Claude Code 中配置分层的开发规范体系。

---

## 📚 规范体系说明

### 三层规范架构

```
┌─────────────────────────────────────┐
│   项目级规范 (claude.md)           │  ← 最高优先级
│   - 项目特定需求                    │
│   - 业务规则                        │
│   - 团队约定                        │
├─────────────────────────────────────┤
│   技术栈规范                        │  ← 中等优先级
│   - stack-react.md                  │
│   - stack-vue.md                    │
│   - stack-nodejs.md                 │
│   - ... 其他技术栈                  │
├─────────────────────────────────────┤
│   全局规范 (global-rules.md)      │  ← 基础优先级
│   - 通用编程原则                    │
│   - 代码风格                        │
│   - Git 规范                        │
│   - 测试要求                        │
└─────────────────────────────────────┘
```

### 优先级规则
- 项目规范 > 技术栈规范 > 全局规范
- 上层规范可以覆盖下层规范
- 未定义的部分继承下层规范

---

## 🚀 快速开始

### 步骤 1: 创建全局配置目录

```bash
# 创建配置目录
mkdir -p ~/.claude

# 或者如果你想更有组织
mkdir -p ~/.claude/stacks
mkdir -p ~/.claude/templates
```

### 步骤 2: 放置规范文件

将生成的规范文件放到对应位置:

```bash
~/.claude/
├── global-rules.md              # 全局通用规范
├── stacks/                       # 技术栈规范目录
│   ├── stack-react.md
│   ├── stack-vue.md
│   ├── stack-nodejs.md
│   └── stack-python.md          # 可自行添加
└── templates/                    # 项目模板目录
    └── project-template-claude.md
```

### 步骤 3: 在项目中创建 claude.md

```bash
# 进入你的项目目录
cd /path/to/your/project

# 复制模板
cp ~/.claude/templates/project-template-claude.md ./claude.md

# 编辑模板,填入项目特定信息
vim claude.md  # 或使用你喜欢的编辑器
```

### 步骤 4: 在 claude.md 中引用规范

在项目的 `claude.md` 文件开头添加:

```markdown
# 项目开发规范 - 我的项目

> 继承规范:
> 1. 全局规范: `~/.claude/global-rules.md`
> 2. React 规范: `~/.claude/stacks/stack-react.md`
> 3. 本项目特定规范 (见下文)

---

## 项目特定规范

[这里写项目特定的规范...]
```

---

## 📝 使用 Claude Code

### 方法 1: 自动读取 (推荐)

Claude Code 会自动读取项目根目录的 `claude.md` 文件。

```bash
# 在项目目录中启动 Claude Code
cd /path/to/your/project
claude-code

# Claude Code 会自动读取 ./claude.md
```

### 方法 2: 在对话中明确引用

如果需要,你可以在对话中明确让 Claude 参考规范:

```
请按照项目的 claude.md 和全局规范来实现用户注册功能。
```

### 方法 3: 使用 .claudefiles

创建 `.claudefiles` 文件,让 Claude 总是能看到规范:

```bash
# 在项目根目录创建 .claudefiles
echo "claude.md" > .claudefiles
echo "~/.claude/global-rules.md" >> .claudefiles
```

---

## 🎯 实际使用示例

### 示例 1: 创建 React 项目

```bash
# 1. 创建项目
npm create vite@latest my-app -- --template react-ts

# 2. 进入项目
cd my-app

# 3. 复制并编辑 claude.md
cp ~/.claude/templates/project-template-claude.md ./claude.md

# 4. 在 claude.md 中指定技术栈
# 确保包含以下内容:
```

```markdown
# 规范继承关系

1. 本文档 (项目特定规范) - 最高优先级
2. React 技术栈规范 - `~/.claude/stacks/stack-react.md`
3. 全局规范 - `~/.claude/global-rules.md`
```

```bash
# 5. 启动 Claude Code
claude-code
```

### 示例 2: 创建 Node.js API 项目

```bash
# 1. 创建项目目录
mkdir my-api && cd my-api
npm init -y

# 2. 复制并编辑 claude.md
cp ~/.claude/templates/project-template-claude.md ./claude.md

# 3. 在 claude.md 中指定技术栈
```

```markdown
# 规范继承关系

1. 本文档 (项目特定规范)
2. Node.js 技术栈规范 - `~/.claude/stacks/stack-nodejs.md`
3. 全局规范 - `~/.claude/global-rules.md`
```

### 示例 3: 全栈项目 (前后端)

```markdown
# 规范继承关系

## 前端规范
1. 本文档前端部分
2. React 规范 - `~/.claude/stacks/stack-react.md`
3. 全局规范 - `~/.claude/global-rules.md`

## 后端规范
1. 本文档后端部分
2. Node.js 规范 - `~/.claude/stacks/stack-nodejs.md`
3. 全局规范 - `~/.claude/global-rules.md`
```

---

## 🔧 高级配置

### 自定义技术栈规范

如果你使用其他技术栈,可以创建自己的规范:

```bash
# 创建 Python/Django 规范
touch ~/.claude/stacks/stack-python.md

# 创建 Flutter 规范
touch ~/.claude/stacks/stack-flutter.md

# 创建 Go 规范
touch ~/.claude/stacks/stack-go.md
```

参考现有的规范文件结构来编写。

### 团队共享规范

#### 方法 1: 使用 Git 仓库

```bash
# 1. 创建规范仓库
mkdir coding-standards && cd coding-standards
git init

# 2. 添加规范文件
cp ~/.claude/global-rules.md ./
cp -r ~/.claude/stacks ./

# 3. 推送到远程
git add .
git commit -m "Initial coding standards"
git push origin main

# 4. 团队成员克隆
git clone <repo-url> ~/.claude
```

#### 方法 2: 使用符号链接

```bash
# 团队共享目录
ln -s /path/to/shared/standards ~/.claude
```

### 项目模板管理

创建不同类型项目的模板:

```bash
~/.claude/templates/
├── react-app-template.md
├── vue-app-template.md
├── nodejs-api-template.md
├── nextjs-app-template.md
└── fullstack-template.md
```

使用时:

```bash
cp ~/.claude/templates/react-app-template.md ./claude.md
```

---

## 💡 最佳实践

### 1. 保持规范简洁
- 全局规范: 只包含通用原则
- 技术栈规范: 只包含该技术栈特有内容
- 项目规范: 只包含项目特定需求

### 2. 定期更新规范
```bash
# 每月审查一次规范
cd ~/.claude
git log  # 查看变更历史
```

### 3. 文档化决策
在项目 claude.md 中记录重要决策:

```markdown
## 技术决策记录

### ADR-001: 选择 Zustand 而不是 Redux
日期: 2024-12-16
决策: 使用 Zustand 作为状态管理方案
原因:
- 更简洁的 API
- 更小的包体积
- 足够满足项目需求
```

### 4. 使用清单
在项目 claude.md 中添加检查清单:

```markdown
## 发布前检查清单
- [ ] 所有测试通过
- [ ] 代码覆盖率 ≥ 80%
- [ ] 无 ESLint 错误
- [ ] 更新了 CHANGELOG
- [ ] 更新了文档
```

---

## 🐛 常见问题

### Q: Claude Code 没有读取到规范文件?

**A:** 确保:
1. 文件路径正确
2. 文件有读取权限
3. 在项目根目录启动 Claude Code
4. claude.md 文件存在于项目根目录

### Q: 多个规范冲突怎么办?

**A:** 按优先级处理:
1. 项目规范优先级最高
2. 技术栈规范次之
3. 全局规范最低
4. 在项目规范中明确说明覆盖了哪些规则

### Q: 如何验证规范生效?

**A:** 在对话中测试:

```
我要创建一个用户组件,请按照项目规范来实现。
```

观察 Claude 的回答是否符合你的规范。

### Q: 规范文件太长怎么办?

**A:** 
1. 拆分成多个文件
2. 使用目录结构组织
3. 只保留核心规范,细节放到 Wiki

---

## 📚 规范模板清单

完整的规范文件模板:

- ✅ `global-rules.md` - 全局通用规范
- ✅ `stack-react.md` - React 技术栈规范
- ✅ `stack-vue.md` - Vue 技术栈规范
- ✅ `stack-nodejs.md` - Node.js 技术栈规范
- ✅ `project-template-claude.md` - 项目规范模板

你可以根据需要创建:
- `stack-python.md` - Python 规范
- `stack-flutter.md` - Flutter 规范
- `stack-golang.md` - Go 规范
- 等等...

---

## 🎓 下一步

1. **创建全局规范**: 复制 `global-rules.md` 到 `~/.claude/`
2. **选择技术栈规范**: 复制对应的 stack 文件到 `~/.claude/stacks/`
3. **创建项目规范**: 在项目中创建 `claude.md`
4. **开始使用**: 用 Claude Code 开发,让 AI 遵循你的规范!

---

## 📖 相关资源

- [Claude Code 文档](https://docs.anthropic.com/claude-code)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/)

---

## 🤝 贡献

如果你创建了新的技术栈规范或改进了现有规范,欢迎分享给团队!

---

**最后更新**: 2024-12-16
**维护者**: Sggmico